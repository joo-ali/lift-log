# 📖 الدليل الشامل لمشروع Lift Log (من الصفر للاحتراف)

يا بطل، الملف ده هو "الكتالوج" بتاعك. لو تهت أو نسيت الملف ده بيعمل إيه، ارجع هنا. الأبلكيشن متقسم طبقات (Layers) عشان يكون منظم وسهل التعديل.

---

## 📂 1. تشريح الفولدرات والملفات (File-by-File)

### 📁 lib/core (القلب المشغل)
ده فيه الحاجات اللي بتخدم الأبلكيشن كله:
*   **`di/service_locator.dart`**: ده "المدير". بنسجل فيه كل الـ Repos والـ Cubits عشان نستخدمهم بكلمة `sl()`.
*   **`routes/app_router.dart`**: خريطة الطرق. فيها كل الشاشات وازاي ننتقل بينهم.
*   **`theme/`**: فيها `app_theme.dart` (الألوان والخطوط) و `theme_cubit.dart` (للتبديل بين Dark/Light mode).
*   **`services/firebase_auth_service.dart`**: المترجم اللي بيكلم Firebase عشان الـ Login والـ Register.
*   **`services/hive_service.dart`**: المسؤول عن فتح وتجهيز الداتا بيز المحلية (Hive).
*   **`localization/`**: فيها `locale_cubit.dart` المسؤول عن قلب الأبلكيشن (عربي/إنجليزي).
*   **`widgets/`**: جواه الـ Widgets المشتركة (Custom Buttons, Custom TextFields).

### 📁 lib/data (مخازن البيانات)
*   **`models/`**: قوالب البيانات (User, Workout, Exercise). فيها وظائف `fromMap` و `toMap` للتحويل بين الكود والداتا بيز.
*   **`data_sources/local/`**: الكود اللي بيكتب ويمسح في موبايلك مباشرة (Hive).

### 📁 lib/features (المميزات)
الأبلكيشن متقسم مميزات (Auth, Home, Workout, etc). كل واحدة جواها:
*   **`presentation/`**: الشاشات (Pages) والحتت الصغيرة (Widgets).
*   **`cubit/`**: المخ اللي بيفكر للشاشة دي بس.
*   **`data/`**: الـ Repository اللي بيقرر يجيب الداتا منين.

---

## ❓ 2. ليه بنستخدم الـ Repository؟ (لغز المدير والموظف)

ده أهم سؤال: "ليه الـ Cubit ما يكلمش الداتا بيز علطول؟". تعال نفهمها بمثال **المطعم**:
*   **الـ UI (الزبون):** هو اللي بيطلب الأكل.
*   **الـ Cubit (الويتر):** هو اللي بياخد الطلب ويوصله للمطبخ.
*   **الـ Repository (مدير المطبخ):** هو اللي بيقرر: "هل عندنا الأكل ده في الثلاجة (**Hive/Local**)؟ ولا نطلبه من المورد (**Firebase/Cloud**)؟". الويتر (Cubit) ملوش دعوة الأكل جه منين، هو بياخد الطبق جاهز ويطلعه للزبون.

---

## 🛠️ 3. شرح الكود والحاجات "الغريبة"

### 🚀 الـ Cubit (إزاي بيفكر؟)
في الكود، هتلاقي الـ Cubit دايماً بيعمل `emit`. يعني إيه؟
*   لما يبدأ يحمل: `emit(StateLoading())` -> الشاشة تظهر علامة تحميل.
*   لما يخلص بنجاح: `emit(StateSuccess(data))` -> الشاشة تظهر الداتا.
*   لما يحصل غلط: `emit(StateError(message))` -> الشاشة تظهر رسالة خطأ.

### 🎨 الـ Widgets والتقنيات المميزة
في المشروع ده بنستخدم حاجات بتخلي الكود "شيك" واحترافي:
1.  **`ScreenUtilInit`**: موجودة في `main.dart`. بتخلي الأبعاد (العرض والطول) تتغير حسب حجم موبايلك، يعني الأبلكيشن ميبوظش لو الموبايل صغير.
2.  **`MultiBlocProvider`**: بنحط فيها كل الـ Cubits في بداية الأبلكيشن عشان يبقوا جاهزين لأي شاشة تطلبهم.
3.  **`HiveType` & `HiveField`**: دول "استيكرات" بنحطها فوق الموديلات (Models) عشان Hive يعرف يسيفهم في الموبايل.
4.  **`AppLocalizations`**: دي اللي بتخلينا نكتب `context.l10n.welcome` وتظهر بالعربي أو بالإنجليزي حسب اللغة المختارة.

---

## 🎬 4. سيناريو عملي (قصة حياة تمرينة)

1.  **الـ UI:** الزبون يدوس "Save". الشاشة تنادي الويتر (Cubit).
2.  **الـ Cubit:** الويتر يرفع يافطة "جاري التحميل" (**Loading**) ويكلم المدير (Repo).
3.  **الـ Repository:** المدير يحفظ نسخة في ثلاجة الموبايل (**Hive**) ويرفع نسخة على السحاب (**Firebase**).
4.  **الـ Cubit:** أول ما المدير يخلص، الويتر يشيل يافطة التحميل ويحط "تم النجاح" (**Success**).
5.  **الـ UI:** الزبون (أنت) يشوف رسالة "عاش يا بطل!" والشاشة ترجعك للرئيسية.

---

## 🏗️ 5. التحديثات الأخيرة (الميزات المتقدمة)

### 🔐 عزل بيانات المستخدمين (Multi-user Isolation)
عشان الأبلكيشن يدعم أكتر من مستخدم وكل واحد يشوف تمارينوا بس:
1.  **الـ Cubits**: بقوا بيعتمدوا على `AuthCubit`. أول ما بتفتح شاشة، الـ Cubit بيسأل الـ `AuthCubit`: "مين المستخدم اللي مسجل دخول دلوقتي؟" وبناءً عليه بيجيب الداتا بتاعته بس.
2.  **الـ Models**: ضيفنا حقل `userId` في `WorkoutModel`. أي تمرينة بتتسيف، بتبقى "مختومة" بـ ID صاحبها.

### ☁️ المزامنة السحابية (Cloud Sync)
دلوقتي تمارينك مش بس على الموبايل:
*   الـ `WorkoutRepository` بقى بيبعت نسخة لـ **Firestore** أول ما تخلص التمرين.
*   لو مسحت الأبلكيشن ونزلته تاني، بمجرد ما تعمل Login، الأبلكيشن بيعمل `syncWorkoutsFromCloud` وبيرجعلك تاريخك كله.

### 🔄 نظام الجداول (Routines & Splits)
دي الميزة اللي بتوفر وقت على البطل:
*   **إزاي بتشتغل؟**: الأبلكيشن بيدور في تاريخك القديم. لو لقى إنك لعبت "Leg Day" قبل كدة، بيظهرلك "Chip" (زرار صغير) باسم "Leg Day".
*   **التكرار الذكي**: أول ما تدوس على الجدول، الأبلكيشن بيملا الشاشة أوتوماتيك بآخر أوزان وعدات عملتها في اليوم ده قبل كدة، وبيرجع الـ `isDone` لـ `false` عشان تبدأ تمرينة جديدة بنفس القوة.
*   **المصادر**: الجداول بتيجي من مكانين: جداول عالمية (System Routines) أو جداولك الشخصية اللي أنت صممتها ولعبتها قبل كدة.

---

### 💡 نصيحة أخيرة:
لو تهت في الكود، اسأل نفسك: "أنا دلوقتي بغير في شكل (UI)؟ ولا في تفكير (Cubit)؟ ولا في مخزن (Data)؟" وهتلاقي نفسك وصلت للفولدر الصح فوراً.
