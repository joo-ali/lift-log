# 📖 الدليل الهندسي الشامل لمشروع Lift Log (Engineering Guide)

هذا الدليل يشرح بالتفصيل الممل المعمارية المستخدمة، التقنيات المختارة، والمنطق البرمجي وراء كل قرار تقني.

---

## 🏗️ 1. فلسفة المعمارية (Clean Architecture)

لقد اخترنا تقسيم المشروع لطبقات (Layers) واضحة جداً:
*   **Core:** يحتوي على "عظام" التطبيق (الألوان، الخطوط، المسارات، والـ Widgets العامة).
*   **Data:** مخازن البيانات والـ Repositories.
*   **Features:** كل ميزة (Auth, Home, Workout) منعزلة تماماً، مما يسهل على الفريق العمل في وقت واحد دون تداخل.

### ❓ لماذا استخدمنا الـ Repository Pattern؟
بدلاً من أن نكلم Firebase مباشرة من الـ UI، وضعنا الـ Repository كـ "مدير عمليات". هو الذي يقرر:
- هل نستخدم **Hive** (محلي) لأن الإنترنت مقطوع؟
- هل نستخدم **Firestore** (سحابي) لمزامنة البيانات؟
- الـ UI لا يشغل باله بهذه التفاصيل، هو فقط يطلب `getWorkouts()`.

---

## 🛠️ 2. نظام التجاوب (ScreenUtil Deep Dive)

### ❓ لماذا `flutter_screenutil` بدل الـ `Media Query`؟
في تطبيقات اللياقة البدنية، نحتاج لدقة شديدة في توزيع العناصر (الكروت، الرسوم البيانية).
*   **الـ `.w` والـ `.h`:** تضمن أن المسافات (Padding/SizedBox) يتم حسابها كنسبة مئوية من حجم تصميم Figma الأصلي، وليس فقط نسبة من عرض الشاشة.
*   **الـ `.sp`:** تعالج مشكلة "Accessibility"؛ لو المستخدم كبّر خط موبايله من الإعدادات، الـ `.sp` تضمن ألا ينكسر التصميم.
*   **الـ `.r`:** تضمن أن استدارة الحواف (Radius) تظل متناسقة مع كبر حجم الكارت.

---

## 💾 3. استراتيجية البيانات الهجينة (Hybrid Storage)

التطبيق يستخدم 3 مستويات من التخزين:
1.  **Hive (Zero-Latency):** قاعدة بيانات NoSQL سريعة جداً. تُستخدم لحفظ التمارين والمجموعات "لحظياً". المستخدم يسجل وزنه، يذهب للـ Hive فوراً.
2.  **Firebase Firestore (Global Sync):** للمزامنة. أي تغيير في Hive، نحاول رفعه لـ Firebase في الخلفية (Background Sync).
3.  **MockAPI (Remote Config):** لجلب "Routine Splits" (مثل Push/Pull/Legs). هذا يسمح لنا بتحديث الجداول دون تحديث التطبيق على المتجر.

---

## 🎨 4. نظام السمات (Theme Engine)

تم بناء نظام ثيمات مرن جداً في `core/theme`:
*   استخدام **ThemeCubit** لحفظ حالة الـ Dark Mode في الـ Hive.
*   جميع الـ Custom Widgets تقرأ ألوانها من `Theme.of(context)`؛ لذا بمجرد تغيير اللون الأساسي في `AppColors` يتغير في التطبيق بالكامل.

---

## ⚙️ 5. حقن التبعيات (Dependency Injection - GetIt)
نستخدم `sl()` (Service Locator) للوصول للـ Cubits والـ Repositories.
*   **الميزة:** نضمن وجود نسخة واحدة (Singleton) من الـ `AuthRepository` مثلاً، مما يوفر استهلاك الرامات ويمنع تضارب البيانات.
