# 🛠️ التقرير التقني التفصيلي للمحرك الأساسي (Mazen & Kero)

هذا التقرير هو المرجع الفني لمناقشة الجزء الخاص بـ (Mazen & Kero). يوضح كل أداة استُخدمت، الأسباب التقنية للاختيار، التحديات (المشاكل) التي واجهتنا، وكيفية التغلب عليها.

---

## 🏗️ القسم الأول: مازن (Home & Data Architecture)

### 1. شاشة الصفحة الرئيسية (`home_screen.dart`)
*   **التقنيات:** `ListView.builder`, `RefreshIndicator`, `BlocBuilder`.
*   **لماذا؟** الـ `ListView.builder` بيعمل Lazy Loading للأصناف، يعني بيبني اللي ظاهر على الشاشة بس، وده بيوفر في الـ Memory جداً.
*   **المميزات:** أداء سلس وسريع (Smooth Scrolling).
*   **العيوب:** إدارة المسافات (Padding) بين العناصر في الـ ListView ممكن تكون خادعة في الـ UI.
*   **المشاكل والحلول:** واجهتنا مشكلة إن الـ Shadow بتاع الكروت كان بيتقص (Clipped). **الحل:** استخدمنا `clipBehavior: Clip.none` في الـ ListView عشان الـ Shadows تظهر كاملة وبشكل جمالي.

### 2. منطق البيانات (`home_cubit.dart` & `home_repository.dart`)
*   **التقنيات:** `Cubit` مع استراتيجية **"Cache-Then-Network"**.
*   **لماذا؟** اخترنا الـ `Cubit` لبساطته وسرعة استجابته مقارنة بالـ Bloc الكامل. الـ Repository بيجيب من `Hive` أولاً عشان المستخدم ميسناش ثانية واحدة (Zero Latency)، وبعدين بيحدث من `Firestore`.
*   **المميزات:** تجربة مستخدم (UX) ممتازة حتى لو الإنترنت ضعيف جداً.
*   **العيوب:** زيادة تعقيد الكود في الـ Repository بسبب التعامل مع مصدرين للبيانات (Local & Remote).
*   **المشاكل والحلول:** مزامنة بيانات الوزن بين الـ Profile والـ Home. **الحل:** عملنا `Stream` بيراقب التغييرات وبيهدي الـ UI فوراً.

---

## 📊 القسم الثاني: كيرو (Workout Engine & Analytics)

### 1. شاشة إضافة التمرين (`add_workout_screen.dart`)
*   **التقنيات:** `TextEditingController` مخصص لكل Set، و `Uuid` لتوليد معرفات فريدة.
*   **لماذا؟** عشان نقدر نتحكم في كل عدّة وكل وزن بشكل منفصل ونحفظهم في قاعدة البيانات كـ Objects مستقلة.
*   **المميزات:** مرونة كاملة للمستخدم (يقدر يمسح مجموعة، يزود تمرين، أو يغير اسم الجلسة).
*   **العيوب:** إدارة الـ Controllers دي كلها بياخد مجهود من المعالج (CPU).
*   **المشاكل والحلول:** مشكلة الـ **Keyboard Overlap** (الكيبورد كان بيغطي على الخانات الأخيرة). **الحل:** استخدمنا `SingleChildScrollView` مع `EdgeInsets.only(bottom: MediaQuery.of(context).viewInsets.bottom)` عشان الشاشة تترفع لفوق لما الكيبورد يفتح.

### 2. الرسوم البيانية (`custom_line_chart.dart`)
*   **التقنيات:** `CustomPainter` و `Canvas API`.
*   **لماذا؟** المكتبات الجاهزة (زي FlChart) تقيلة وبتحتاج إعدادات كتير عشان نوصل للشكل الـ Minimalist اللي عايزينه. الـ `CustomPainter` خفيف جداً (Zero performance hit).
*   **المميزات:** تحكم كامل في الألوان (Linear Gradients) وانحناء الخطوط.
*   **العيوب:** بيحتاج حسابات رياضية معقدة (Geometry) عشان النقط تطلع في مكانها الصح بناءً على الـ Volume.
*   **المشاكل والحلول:** الـ Chart كان بيطلع بره الشاشة (Overflow) في الموبايلات الصغيرة. **الحل:** استخدمنا `LayoutBuilder` عشان ناخد الـ Constraints بتاع الشاشة ونرسم الـ Chart جواها بالظبط بالـ Pixel.

### 3. جداول التمارين المقترحة (`routine_repository.dart`)
*   **التقنيات:** `http` package للتعامل مع **MockAPI**.
*   **لماذا؟** عشان نفصل التمارين الثابتة عن الكود. لو حبينا نغير التمارين المقترحة، بنغيرها من السيرفر والتطبيق بيحدث نفسه عند المستخدم فوراً.
*   **المميزات:** إمكانية الـ Dynamic Updates.
*   **المشاكل والحلول:** الـ API ساعات كان بيتأخر. **الحل:** عملنا `DefaultRoutines` ثابتة جوه الكود تشتغل كـ "Fallback" لو الـ API فشل أو مفيش إنترنت.

---

## 📱 التصميم المتجاوب (Responsive Philosophy)
*   **الأداة:** `flutter_screenutil`.
*   **ليه استخدمناها؟** عشان نهرب من مشكلة إن التطبيق شكله يتغير بين الأندرويد والآيفون أو الموبايلات الكبيرة والصغيرة.
*   **المميزات:** الكود مكتوب مرة واحدة وبيرندر صح في كل حتة باستخدام الـ Scaling Factor.
*   **المشاكل:** الـ `150.h` في الـ Suggested Routines كانت كبيرة في بعض الشاشات. **الحل:** عملنا `Flexible` و `Expanded` جوه الكارت عشان العناصر تتوزع صح مهما كان الارتفاع المتاح.
