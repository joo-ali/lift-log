# 👤 التقرير التقني لملف المستخدم والجماليات (Abdo)

هذا الملف يوضح القرارات الهندسيّة التي اتخذها عبده في شاشة الملف الشخصي، مع شرح للمميزات، العيوب، وكيفية التغلب على المشاكل التقنية.

---

## 👤 أولاً: إدارة الملف الشخصي (Profile UI & Logic)

### 1. شاشة البروفايل (`profile_screen.dart`)
*   **استخدمت إيه؟** `SingleChildScrollView` مع تقسيم الشاشة لمكونات صغيرة (Widgets) مثل `ProfileStatsGrid` و `ProfileGoalCard`.
*   **ليه؟** لضمان سهولة صيانة الكود (Maintainability). بدلاً من كتابة 500 سطر في ملف واحد، تم تقسيمها لـ Widgets مستقلة.
*   **المميزات:** استجابة كاملة (Responsiveness) باستخدام `ScreenUtil`.
*   **العيوب:** كثرة الـ `Rebuilds` عند تحديث أي بيان صغير، وحلينا ده باستخدام `BlocBuilder` لضمان تحديث الجزء المطلوب فقط.

### 2. تعديل البيانات (`CustomDialog` & `ImagePicker`)
*   **استخدمت إيه؟** مكتبة `image_picker` لرفع الصور، و `CustomDialog` لإدخال الوزن والاسم.
*   **ليه؟** لتوفير تجربة مستخدم سلسة (UX) بحيث لا يضطر المستخدم لمغادرة الشاشة لتعديل بياناته.
*   **المشاكل:** الصور ذات الجودة العالية كانت بتبطئ التطبيق.
*   **الحل:** ضبطت الـ `imageQuality` لـ 70 في الـ `ImagePicker` لتقليل الحجم مع الحفاظ على الوضوح.

---

## 🌍 ثانياً: نظام اللغات (Localization)

### 1. الـ Locale Cubit (`locale_cubit.dart`)
*   **استخدمت إيه؟** `flutter_localizations` مع تخزين `Hive`.
*   **ليه؟** لدعم العربية والإنجليزية بشكل احترافي.
*   **المميزات:** التطبيق بيغير اتجاهه بالكامل (RTL/LTR) تلقائياً.
*   **العيوب:** الحاجة لتعريف كل جملة في ملفين (`app_en.arb` و `app_ar.arb`) مما يزيد وقت التطوير، لكنه يسهل الانتشار العالمي للتطبيق.

---

## 🎨 ثالثاً: إدارة السمات (Theme Management)

### 1. الـ Theme Cubit (`theme_cubit.dart`)
*   **استخدمت إيه؟** `ThemeMode` مع `AppTheme` مركزي.
*   **ليه؟** لتمكين الـ Dark Mode وحفظ اختيار المستخدم في الـ `Hive`.
*   **المميزات:** راحة للعين وتوفير للبطارية في الشاشات الـ OLED.
*   **المشاكل:** بعض الألوان لم تكن واضحة في الـ Dark Mode.
*   **الحل:** استخدمنا `theme.colorScheme.onSurface` بدلاً من الألوان الثابتة لضمان التباين (Contrast) التلقائي.

---

## 📱 الرد على أسئلة المناقشة (عبده):
*   **س: ليه استخدمت Hive مش SharedPreferences؟**
    *   **ج:** لأن Hive أسرع بكتير (NoSQL) وبيدعم تخزين الكائنات (Objects) والـ Lists بسهولة، وده اللي بنحتاجه لحفظ حالة الثيم واللغة بسرعة البرق.
*   **س: إزاي الـ Profile بيحدث البيانات فوراً؟**
    *   **ج:** عن طريق الـ `ProfileCubit` اللي بيبعت `ProfileLoaded` state أول ما الـ Repository يخلص تحديث في Firebase والـ Hive مع بعض.
