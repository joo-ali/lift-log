# 🔐 التقرير التقني لنظام الحماية والمصادقة (Yousef & Ziad)

هذا الملف يحتوي على الأسرار التقنية لنظام الـ Auth، وكيف تم تأمين التطبيق وضمان صحة البيانات المدخلة.

---

## 🔐 أولاً: قسم يوسف (Authentication & Routing)

### 1. نظام الـ Auth Cubit (`auth_cubit.dart`)
*   **استخدمت إيه؟** `BlocListener` و `Cubit`.
*   **ليه؟** لإدارة حالة المستخدم (داخل ولا خارج) بشكل مركزي يأثر على التطبيق كله، واستخدمنا الـ Listener في الـ UI عشان نعمل Navigation فوراً أول ما الحالة تتغير.
*   **المميزات:** أمان عالي، الـ UI مبيعرفش "كلمة السر"، هو بس بيعرف الحالة (Loading, Success, Error).
*   **العيوب:** الـ Session لو انتهت فجأة بنحتاج نعالج الـ "Token Expiration"، وحلينا ده بالربط المباشر مع Firebase Stream.

### 2. خدمة فيربيز (`firebase_auth_service.dart`)
*   **استخدمت إيه؟** `FirebaseAuth` Instance.
*   **ليه؟** عشان دي الطريقة الرسمية والأكثر أماناً، وبتدعم الـ Social Login (زي جوجل) والتحقق من الإيميل.
*   **المميزات:** تشفير كامل للبيانات، إدارة كلمات السر مش مسؤوليتنا (Firebase بيشيلها عننا).
*   **المشاكل:** التعامل مع أخطاء الـ Firebase (زي Weak Password أو Email already in use) وترجمتها لرسائل يفهمها المستخدم العربي أو الإنجليزي.

### 3. نظام المسارات (`app_router.dart`)
*   **استخدمت إيه؟** `OnGenerateRoute`.
*   **ليه؟** عشان نتحكم في مين يدخل فين. لو المستخدم مش مسجل دخول، الـ Router بيرجعه لصفحة الـ Login أوتوماتيكياً.
*   **المميزات:** تنظيم الكود ومنع الدخول العشوائي للشاشات (Security Middleware).

---

## 📝 ثانياً: قسم زياد (Register & Form Logic)

### 1. شاشة التسجيل (`register_screen.dart`)
*   **استخدمت إيه؟** `Form` مع `GlobalKey<FormState>`.
*   **ليه؟** عشان منسمحش للمستخدم يضغط "Register" إلا لو كل الخانات مكتوبة صح (Validation).
*   **المميزات:** تجربة مستخدم نظيفة ومنظمة.
*   **العيوب:** كثرة الخانات (اسم، وزن، إيميل) ممكن تزهق المستخدم، وحلينا ده بتصغير حجم الـ Widgets باستخدام `ScreenUtil`.

### 2. نظام التحقق (`validators.dart`)
*   **استخدمت إيه؟** `RegExp` (Regular Expressions).
*   **ليه؟** للتأكد إن الإيميل فيه `@` وإن كلمة السر فيها أرقام وحروف عشان نرفع مستوى الأمان.
*   **المميزات:** بيمنع الـ "Garbage Data" إنها توصل لقاعدة البيانات.
*   **المشاكل:** الـ Logic بتاع الـ Validation ممكن يكون معقد في الـ Testing، بس فصلناه في ملف لوحده عشان نستخدمه في الـ Login والـ Register مع بعض (Reusability).

### 3. مستودع المصادقة (`auth_repository.dart`)
*   **استخدمت إيه؟** `Repository Pattern`.
*   **ليه؟** عشان نفصل بين "مصدر البيانات" (Firebase) وبين الـ "Logic" (Cubit). لو بكره قررنا نغير Firebase ونستخدم API خاص بنا، هنغير في الملف ده بس!
*   **المميزات:** كود مرن جداً وقابل للتوسعة (Scalable).

---

## 📱 الرد على أسئلة "إزاي الـ UI بيفضل Responsive؟"
*   **التحدي:** في شاشة الـ Register، الخانات كتير، إزاي مش بتعمل Overflow لما الكيبورد يفتح؟
*   **الحل:** استخدمنا `SingleChildScrollView` ولفينا الـ Column بيها، ومع الـ `SizedBox(height: 20.h)` المسافات بتصغر وتكبر حسب حجم الشاشة المتاح، فمستحيل يحصل Overflow.
